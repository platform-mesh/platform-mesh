go 1.26.4

use (
	./apis
	./cmd/jl
	./cmd/qbrtool
	./cmd/release
	./golang-commons
	./operators/account-operator
	./operators/backup-operator
	./operators/extension-manager-operator
	./operators/kcp-migration-operator
	./operators/kro-composition-operator
	./operators/platform-mesh-deployer
	./operators/platform-mesh-operator
	./operators/resource-broker
	./operators/search-operator
	./operators/security-operator
	./operators/terminal-controller-manager
	./services/iam-service
	./services/kubernetes-graphql-gateway
	./services/rebac-authz-webhook
	./services/search-service
	./services/virtual-workspaces
	./subroutines
)

// kcp-dev/multicluster-provider has an internal replace directive that breaks go work sync
replace github.com/kcp-dev/multicluster-provider/client => github.com/kcp-dev/multicluster-provider/client v0.8.1-0.20260714141811-833f063357f8

// Force to use a newer version that is compatible with the newer genproto multi-module approach;
// See https://github.com/googleapis/go-genproto/issues/1015 for more information. This replace
// can be removed if none of our dependencies require both the old and new modules.
replace google.golang.org/genproto => google.golang.org/genproto v0.0.0-20230525234044-86246a831fb4

// Requires change to be merged upstream
replace github.com/kcp-dev/kcp-operator/sdk => github.com/ntnn/kcp-operator/sdk v0.8.1-0.20260807065629-b3aeb506baee

// The deployer runs kcp-operator's controller groups in its own manager, so it
// consumes the operator itself and not only its sdk. Both have to be the same
// commit: the controllers compile against their own sdk revision.
replace github.com/kcp-dev/kcp-operator => github.com/ntnn/kcp-operator v0.8.1-0.20260807065629-b3aeb506baee
