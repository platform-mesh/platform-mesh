# Copyright The Platform Mesh Authors.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

apiVersion: apis.kcp.io/v1alpha1
kind: APIResourceSchema
metadata:
  name: v260825-f6a06120.marketplaceentries.marketplace.platform-mesh.io
spec:
  group: marketplace.platform-mesh.io
  names:
    kind: MarketplaceEntry
    listKind: MarketplaceEntryList
    plural: marketplaceentries
    singular: marketplaceentry
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      description: MarketplaceEntry is the Schema for the marketplaceentries API.
      properties:
        apiVersion:
          description: |-
            APIVersion defines the versioned schema of this representation of an object.
            Servers should convert recognized schemas to the latest internal value, and
            may reject unrecognized values.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
          type: string
        kind:
          description: |-
            Kind is a string value representing the REST resource this object represents.
            Servers may infer this from the endpoint the client submits requests to.
            Cannot be updated.
            In CamelCase.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
          type: string
        metadata:
          type: object
        spec:
          description: MarketplaceEntrySpec defines the desired state of MarketplaceEntry.
          properties:
            apiBindingName:
              description: |-
                APIBindingName is the metadata.name of the APIBinding backing this installation.
                Empty means not installed.
              type: string
            apiExport:
              description: |-
                APIExport is the v1alpha1-compatible export associated with the marketplace entry.
                It is retained for existing Marketplace clients.
              properties:
                apiVersion:
                  type: string
                kind:
                  type: string
                metadata:
                  type: object
                spec:
                  description: Spec holds the desired state.
                  properties:
                    identity:
                      description: Identity points to the Secret containing the API
                        identity.
                      properties:
                        secretRef:
                          description: secretRef is a reference to a secret that contains
                            the API identity in the 'key' file.
                          properties:
                            name:
                              description: name is unique within a namespace to reference
                                a secret resource.
                              type: string
                            namespace:
                              description: namespace defines the space within which
                                the secret name must be unique.
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                    latestResourceSchemas:
                      description: LatestResourceSchemas records the latest APIResourceSchemas
                        exposed by the APIExport.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: set
                    maximalPermissionPolicy:
                      description: MaximalPermissionPolicy sets the APIExport's upper
                        authorization bound.
                      properties:
                        local:
                          description: local is the policy that is defined in same
                            workspace as the API Export.
                          type: object
                      type: object
                    permissionClaims:
                      description: PermissionClaims are the legacy v1alpha1 claims
                        retained for existing clients.
                      items:
                        description: LegacyPermissionClaim is the JSON-compatible
                          v1alpha1 permission claim.
                        properties:
                          all:
                            description: All claims all resources for the group/resource.
                            type: boolean
                          group:
                            default: ""
                            description: Group is the name of an API group. The empty
                              string represents the core group.
                            pattern: ^(|[a-z0-9]([-a-z0-9]*[a-z0-9](\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*)?)$
                            type: string
                          identityHash:
                            default: ""
                            description: IdentityHash identifies the API identity
                              for non-core resources.
                            type: string
                          resource:
                            description: Resource is the name of the resource.
                            pattern: ^[a-z][-a-z0-9]*[a-z0-9]$
                            type: string
                          resourceSelector:
                            description: ResourceSelector is the list of specifically
                              claimed resources.
                            items:
                              properties:
                                name:
                                  description: |-
                                    name of an object within a claimed group/resource.
                                    It matches the metadata.name field of the underlying object.
                                    If namespace is unset, all objects matching that name will be claimed.
                                  maxLength: 253
                                  minLength: 1
                                  pattern: ^([a-z0-9][-a-z0-9_.]*)?[a-z0-9]$
                                  type: string
                                namespace:
                                  description: |-
                                    namespace containing the named object. Matches metadata.namespace field.
                                    If "name" is unset, all objects from the namespace are being claimed.
                                  minLength: 1
                                  type: string
                              type: object
                              x-kubernetes-validations:
                              - message: at least one field must be set
                                rule: has(self.__namespace__) || has(self.name)
                            type: array
                        required:
                        - resource
                        type: object
                        x-kubernetes-validations:
                        - message: either "all" or "resourceSelector" must be set
                          rule: (has(self.all) && self.all) != (has(self.resourceSelector)
                            && size(self.resourceSelector) > 0)
                      type: array
                      x-kubernetes-list-map-keys:
                      - group
                      - resource
                      x-kubernetes-list-type: map
                  type: object
                status:
                  description: Status communicates the observed state.
                  properties:
                    conditions:
                      description: conditions is a list of conditions that apply to
                        the APIExport.
                      items:
                        description: Condition defines an observation of a object
                          operational state.
                        properties:
                          lastTransitionTime:
                            description: |-
                              Last time the condition transitioned from one status to another.
                              This should be when the underlying condition changed. If that is not known, then using the time when
                              the API field changed is acceptable.
                            format: date-time
                            type: string
                          message:
                            description: |-
                              A human readable message indicating details about the transition.
                              This field may be empty.
                            type: string
                          reason:
                            description: |-
                              The reason for the condition's last transition in CamelCase.
                              The specific API may choose whether or not this field is considered a guaranteed API.
                              This field may not be empty.
                            type: string
                          severity:
                            description: |-
                              Severity provides an explicit classification of Reason code, so the users or machines can immediately
                              understand the current situation and act accordingly.
                              The Severity field MUST be set only when Status=False.
                            type: string
                          status:
                            description: Status of the condition, one of True, False,
                              Unknown.
                            type: string
                          type:
                            description: |-
                              Type of condition in CamelCase or in foo.example.com/CamelCase.
                              Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                              can be useful (see .node.status.conditions), the ability to deconflict is important.
                            type: string
                        required:
                        - lastTransitionTime
                        - status
                        - type
                        type: object
                      type: array
                    identityHash:
                      description: |-
                        identityHash is the hash of the API identity key of this APIExport. This value
                        is immutable as soon as it is set.
                      type: string
                    virtualWorkspaces:
                      description: |-
                        virtualWorkspaces contains all APIExport virtual workspace URLs.

                        Deprecated: use APIExportEndpointSlice.status.endpoints instead
                      items:
                        properties:
                          url:
                            description: url is an APIExport virtual workspace URL.
                            minLength: 1
                            type: string
                        required:
                        - url
                        type: object
                      type: array
                  type: object
              type: object
            apiExportPermissionClaims:
              description: |-
                APIExportPermissionClaims contains v1alpha2 permission claims associated with
                the APIExport, including verbs and default selectors. Claims originating from
                v1alpha1 resourceSelector are omitted because they cannot be translated to a
                v1alpha2 label selector without broadening their scope.
              items:
                description: |-
                  PermissionClaim identifies an object by GR and identity hash.
                  Its purpose is to determine the added permissions that a service provider may
                  request and that a consumer may accept and allow the service provider access to.
                properties:
                  defaultSelector:
                    description: |-
                      defaultSelector is the default selector to use when creating APIBindings
                      via WorkspaceType's defaultAPIBindings. If not set, the APIBinding will
                      default to matchAll: true.

                      This allows API providers to suggest a default scope for permission claims
                      that will be used when workspaces are automatically created with default
                      APIBindings. Users can always override the default selector by accepting the
                      claim with a different selector or by manually creating APIBindings with a custom selector.
                    properties:
                      matchAll:
                        description: matchAll grants access to all objects of the
                          claimed resource.
                        type: boolean
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  group:
                    default: ""
                    description: |-
                      group is the name of an API group.
                      For core groups this is the empty string '""'.
                    pattern: ^(|[a-z0-9]([-a-z0-9]*[a-z0-9](\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*)?)$
                    type: string
                  identityHash:
                    default: ""
                    description: |-
                      This is the identity for a given APIExport that the APIResourceSchema belongs to.
                      The hash can be found on APIExport and APIResourceSchema's status.
                      It will be empty for core types.
                      Note that one must look this up for a particular kcp instance.
                    type: string
                  resource:
                    description: |-
                      resource is the name of the resource.
                      Note: it is worth noting that you can not ask for permissions for resource provided by a CRD
                      not provided by an api export.
                    pattern: ^[a-z][-a-z0-9]*[a-z0-9]$
                    type: string
                  verbs:
                    description: |-
                      verbs is a list of supported API operation types (this includes
                      but is not limited to get, list, watch, create, update, patch,
                      delete, deletecollection, and proxy).
                    items:
                      type: string
                    minItems: 1
                    type: array
                    x-kubernetes-list-type: set
                required:
                - resource
                - verbs
                type: object
              type: array
            providerMetadata:
              description: ProviderMetadata contains metadata about the provider of
                the marketplace entry.
              properties:
                apiVersion:
                  description: |-
                    APIVersion defines the versioned schema of this representation of an object.
                    Servers should convert recognized schemas to the latest internal value, and
                    may reject unrecognized values.
                    More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                  type: string
                kind:
                  description: |-
                    Kind is a string value representing the REST resource this object represents.
                    Servers may infer this from the endpoint the client submits requests to.
                    Cannot be updated.
                    In CamelCase.
                    More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                  type: string
                metadata:
                  type: object
                spec:
                  description: ProviderMetadataSpec defines the desired state of ProviderMetadata.
                  properties:
                    contacts:
                      items:
                        properties:
                          displayName:
                            type: string
                          email:
                            type: string
                          role:
                            items:
                              type: string
                            type: array
                        required:
                        - displayName
                        type: object
                      type: array
                    data:
                      description: Additional information that should be stored with
                        the provider metadata.
                      x-kubernetes-preserve-unknown-fields: true
                    description:
                      type: string
                    detailViewExtensions:
                      items:
                        description: |-
                          DetailViewExtension describes a provider-owned micro frontend that enriches
                          the provider details view. The extension is rendered in list order.
                        properties:
                          url:
                            description: URL is the absolute URL of the micro frontend.
                            type: string
                        required:
                        - url
                        type: object
                      type: array
                    displayName:
                      type: string
                    documentation:
                      items:
                        properties:
                          displayName:
                            type: string
                          url:
                            type: string
                        type: object
                      type: array
                    helpCenterData:
                      items:
                        properties:
                          displayName:
                            type: string
                          url:
                            type: string
                        type: object
                      type: array
                    icon:
                      properties:
                        dark:
                          properties:
                            data:
                              type: string
                            url:
                              type: string
                          type: object
                        light:
                          properties:
                            data:
                              type: string
                            url:
                              type: string
                          type: object
                      required:
                      - dark
                      - light
                      type: object
                    links:
                      items:
                        properties:
                          displayName:
                            type: string
                          url:
                            type: string
                        type: object
                      type: array
                    preferredSupportChannels:
                      items:
                        properties:
                          displayName:
                            type: string
                          url:
                            type: string
                        type: object
                      type: array
                    tags:
                      items:
                        type: string
                      type: array
                  required:
                  - displayName
                  type: object
                status:
                  description: ProviderMetadataStatus defines the observed state of
                    ProviderMetadata.
                  type: object
              type: object
          required:
          - apiExport
          - providerMetadata
          type: object
        status:
          description: MarketplaceEntryStatus defines the observed state of MarketplaceEntry.
          type: object
      type: object
    served: true
    storage: true
    subresources:
      status: {}
